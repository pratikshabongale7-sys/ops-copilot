# docker-compose lets you start the service with one command: `docker compose up`.
services:
  api:
    build: .
    ports:
      - "7860:7860"   # host:container — matches the Dockerfile's port. Open
                       # http://localhost:7860  (demo page)  or  /health
    env_file:
      - .env
    restart: unless-stopped
