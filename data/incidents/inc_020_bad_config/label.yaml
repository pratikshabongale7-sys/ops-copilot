incident_id: inc_020
root_cause: bad_config
service: email
onset_minute: 10
split: test
expected_diagnosis: '''email'' began failing almost all requests right after a config
  update (v1.5.5) at minute 10; logs show a missing/invalid env var. Root cause is
  the bad configuration change; recommend reverting it.'
key_signals:
- near-total failure (very high error rate) with a sharp onset
- a config-change deploy (v1.5.5) at the onset
- '''configuration error / missing env var'' in logs'
