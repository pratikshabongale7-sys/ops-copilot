incident_id: inc_011
root_cause: memory_leak_oom
service: shipping
onset_minute: 8
split: train
expected_diagnosis: '''shipping'' memory climbed steadily to its ~1GB limit, then
  the pod was OOMKilled and restarted near minute 27 (memory dropped to baseline,
  errors spiked). A memory leak is the likely cause.'
key_signals:
- memory_mb ramps steadily toward the limit
- sudden memory drop + error spike = an OOM restart
- '''OutOfMemoryError / OOMKilled'' in logs'
